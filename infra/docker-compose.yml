# InsightForge — Local Development Stack
# Usage: docker-compose -f infra/docker-compose.yml up

# TODO: Week 8 — Add Postgres and Chroma services

version: "3.9"

services:
  # Placeholder — services will be added as we build
  app:
    build:
      context: ../backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - ../.env
